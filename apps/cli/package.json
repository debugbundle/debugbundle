{
  "name": "@debugbundle/cli",
  "version": "0.1.7",
  "private": false,
  "description": "Command-line interface for DebugBundle",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/debugbundle/debugbundle",
    "directory": "apps/cli"
  },
  "homepage": "https://debugbundle.com/docs/cli",
  "bugs": {
    "url": "https://github.com/debugbundle/debugbundle/issues"
  },
  "keywords": ["debugbundle", "debugging", "ai-agent", "cli", "incident-response"],
  "engines": {
    "node": ">=24 <25"
  },
  "type": "module",
  "scripts": {
    "start": "tsx src/entrypoint.ts",
    "build": "esbuild src/entrypoint.ts --bundle --platform=node --format=cjs --target=node24 --external:@node-rs/argon2 --outfile=dist/main.cjs",
    "prepack": "npm run build"
  },
  "bin": {
    "debugbundle": "bin/debugbundle.js"
  },
  "files": ["bin", "dist", "README.md", "LICENSE"],
  "dependencies": {
    "@node-rs/argon2": "^2.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "tsx": "^4.20.5"
  }
}
