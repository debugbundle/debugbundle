{
  "mcpServers": {
    "debugbundle": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@debugbundle/mcp@1.6.3"],
      "env": {
        "DEBUGBUNDLE_MEMBER_TOKEN": "${user_config.member_token}",
        "DEBUGBUNDLE_API_URL": "${user_config.api_url}"
      }
    }
  }
}
